extends Node

const TILE_SIZE = 32
const MAP_TILES_X = 400
const MAP_TILES_Y = 400
var MAP_SIZE_X = TILE_SIZE * MAP_TILES_X
var MAP_SIZE_Y = TILE_SIZE * MAP_TILES_Y

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
